# Structs

Structs are Go's primary data modeling mechanism.

Structs:
- group related fields
- support composition
- are copied by value
- can be nested

Structs are similar to classes in some languages, but they contain only data.

Behavior is added later using methods.